# rclone配置示例文件
# 请根据您使用的云存储服务进行配置
# 配置完成后,将此文件重命名为 rclone.conf

# OneDrive配置示例
[onedrive]
type = onedrive
# token会在首次运行rclone config时自动生成
# 请勿手动填写,使用 rclone config 命令进行配置
token = {"access_token":"your_access_token","token_type":"Bearer","refresh_token":"your_refresh_token","expiry":"2026-01-01T00:00:00Z"}
drive_id = your_drive_id
drive_type = personal

# Google Drive配置示例
[gdrive]
type = drive
scope = drive
# 使用API Key进行配置（需要先通过 rclone config 配置OAuth2）
# 请使用 rclone config 命令进行配置，参考: https://rclone.org/drive/
# 配置完成后，rclone会自动生成token
token = {"access_token":"your_access_token","token_type":"Bearer","refresh_token":"your_refresh_token","expiry":"2026-01-01T00:00:00Z"}
client_id = your_client_id
client_secret = your_client_secret

# 其他云存储配置示例(Dropbox等)
# 请参考 rclone 官方文档: https://rclone.org/docs/
